(ns functional-design\chapter3-1)

(defn fibs-work [n i fs]
  (if (= i n)
    fs
    (fibs-work n (inc i) (push fs (apply + (take-last 2 fs))))))

(defn fibs [n]
  (cond
    (< n 1) []
    (= n 1) [1]
    true (fibs-work n 2 [1 1])))

